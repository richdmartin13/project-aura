import '../App.css';
import React from 'react';
import { themes } from '../config/theme';

function Settings() {
  var theme = themes.retro;

  return (
    <h2>Settings</h2>
  );
}

export default Settings;