import '../App.css';
import React from 'react';
import { themes } from '../config/theme';

function Products() {
  var theme = themes.retro;

  return (
    <h2>Products</h2>
  );
}

export default Products;