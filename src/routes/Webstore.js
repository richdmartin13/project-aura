import '../App.css';
import React from 'react';
import { themes } from '../config/theme';

function Webstore() {
  var theme = themes.retro;

  return (
    <div>
    
    </div>
  );
}

export default Webstore;