import '../App.css';
import React from 'react';
import { themes } from '../config/theme';

function Agenda() {
  var theme = themes.retro;

  return (
    <h2>Agenda</h2>
  );
}

export default Agenda;