import '../App.css';
import React from 'react';
import { themes } from '../config/theme';

function Wallet() {
  var theme = themes.retro;

  return (
    <h2>Wallet</h2>
  );
}

export default Wallet;