import '../App.css';
import React from 'react';
import { themes } from '../config/theme';

function NotFound() {
  var theme = themes.retro;

  return (
    <h2>Not Found</h2>
  );
}

export default NotFound;