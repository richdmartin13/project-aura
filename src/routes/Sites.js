import '../App.css';
import React from 'react';
import { themes } from '../config/theme';

function Sites() {
  var theme = themes.retro;

  return (
    <div>
    
    </div>
  );
}

export default Sites;